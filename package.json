
{
  "name": "worldwise-backend",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch data/cities.json --port $PORT --delay 500",
    "dev": "json-server --watch data/cities.json --port 9000 --delay 500"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  }
}
